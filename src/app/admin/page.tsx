import { requireAdmin } from "@/lib/auth"; import { getDatabase } from "@/db"; import { listUsers } from "@/lib/services/users"; import { createInviteAction, toggleUserAction } from "../actions";
export default async function AdminPage({ searchParams }: { searchParams: Promise<{ invite?: string; error?: string }> }) { await requireAdmin(); const users = await listUsers(getDatabase().db); const { invite, error } = await searchParams; const base = process.env.APP_BASE_URL?.replace(/\/$/, "") || ""; return <section><div className="pageTitle"><div><h1>Users</h1><p className="muted">Invite-only access; no public registration.</p></div><form action={createInviteAction}><button type="submit">Create invite</button></form></div>{error && <p className="error">{error}</p>}{invite && <div className="success"><strong>One-time invite:</strong><code>{base}/register?token={invite}</code></div>}<div className="card tableWrap"><table><thead><tr><th>User</th><th>Role</th><th>Status</th><th></th></tr></thead><tbody>{users.map((user) => <tr key={user.id}><td>{user.username}</td><td>{user.role}</td><td>{user.active ? "active" : "disabled"}</td><td>{user.role !== "ADMIN" && <form action={toggleUserAction}><input type="hidden" name="userId" value={user.id}/><input type="hidden" name="active" value={String(!user.active)}/><button className="secondary" type="submit">{user.active ? "Disable" : "Enable"}</button></form>}</td></tr>)}</tbody></table></div></section>; }
