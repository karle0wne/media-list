import { getDatabase } from "@/db";import { serviceState } from "@/db/schema";import { requireAdmin } from "@/lib/auth";import { listUsers } from "@/lib/services/users";import { CopyButton } from "@/components/copy-button";import { createInviteAction,createPasswordResetAction,toggleUserAction } from "../actions";
export default async function AdminPage({searchParams}:{searchParams:Promise<{invite?:string;reset?:string;error?:string}>}){await requireAdmin();const db=getDatabase().db;const users=await listUsers(db);const backup=(await db.select().from(serviceState).limit(1))[0]??null;const{invite,reset,error}=await searchParams;const base=process.env.APP_BASE_URL?.replace(/\/$/,"")||"";const inviteUrl=invite?`${base}/register?token=${invite}`:"";const resetUrl=reset?`${base}/reset-password?token=${reset}`:"";return <section><div className="pageTitle"><div><h1>Users</h1><p className="muted">Accounts are invite-only. Registration and password reset links are one-time credentials.</p></div><form action={createInviteAction}><button type="submit">+ Add user</button></form></div>{error&&<p className="error">{error}</p>}{invite&&<TokenPanel title="One-time registration link" help="Consumed after one successful registration." url={inviteUrl}/>} {reset&&<TokenPanel title="One-time password reset link" help="Bound to one existing account and consumed after password change." url={resetUrl}/>}<div className="adminMeta"><span>Last successful backup</span><strong>{backup?.lastBackupAt?backup.lastBackupAt.toISOString():"Not recorded yet"}</strong></div><div className="tableWrap"><table><thead><tr><th>User</th><th>Role</th><th>Status</th><th>Actions</th></tr></thead><tbody>{users.map(user=><tr key={user.id}><td>{user.username}</td><td>{user.role}</td><td>{user.active?"active":"disabled"}</td><td><div className="adminActions">{user.active&&<form action={createPasswordResetAction}><input type="hidden" name="userId" value={user.id}/><button className="secondary" type="submit">Reset password</button></form>}{user.role!=="ADMIN"&&<form action={toggleUserAction}><input type="hidden" name="userId" value={user.id}/><input type="hidden" name="active" value={String(!user.active)}/><button className="secondary" type="submit">{user.active?"Disable":"Enable"}</button></form>}</div></td></tr>)}</tbody></table></div></section>;}
function TokenPanel({title,help,url}:{title:string;help:string;url:string}){return <div className="invitePanel"><div><strong>{title}</strong><p className="muted">{help}</p><code>{url}</code></div><CopyButton text={url} label="Copy link"/></div>;}
