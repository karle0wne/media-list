import { registerAction } from "../actions";
export default async function RegisterPage({ searchParams }: { searchParams: Promise<{ token?: string; error?: string }> }) { const { token = "", error } = await searchParams; return <section className="narrow"><h1>Accept invite</h1>{error && <p className="error">{error}</p>}{!token ? <p className="error">Invite token is missing.</p> : <form action={registerAction} className="stack card"><input type="hidden" name="token" value={token} /><label>Username<input name="username" minLength={3} maxLength={32} required /></label><label>Password<input name="password" type="password" minLength={10} required /></label><button type="submit">Create account</button></form>}</section>; }
