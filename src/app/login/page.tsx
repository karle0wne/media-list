import {redirect} from "next/navigation";import {currentUser} from "@/lib/auth";import {magicLinkMailConfigured} from "@/lib/mail";import {oidcConfigured} from "@/lib/oidc";import {loginAction,requestMagicLinkAction} from "../actions";
export default async function LoginPage({searchParams}:{searchParams:Promise<{error?:string;magic?:string}>}){if(await currentUser())redirect("/");const{error,magic}=await searchParams;const ssoEnabled=oidcConfigured();const magicEnabled=magicLinkMailConfigured();return <section className="narrow authPage"><h1>Sign in</h1><p className="muted">Access is controlled by the shared IAM service.</p>{error&&<p className="error">{error}</p>}{magic==="sent"&&<p className="success">If that email is allowed, a sign-in link has been sent.</p>}{ssoEnabled&&<div className="stack card authPrimary"><div><strong>Central SSO</strong><p className="muted authHelp">Use the Google identity allowed for this service by central IAM.</p></div><a href="/login/oidc">Continue with SSO</a></div>}{magicEnabled&&<details className="authFallback"><summary>Legacy email link</summary><form action={requestMagicLinkAction} className="stack card"><label>Email<input name="email" type="email" autoComplete="email" required/></label><button type="submit">Email me a sign-in link</button></form></details>}<details className="authFallback" open={!ssoEnabled&&!magicEnabled}><summary>Password sign in</summary><form action={loginAction} className="stack card"><label>Username<input name="username" autoComplete="username" required/></label><label>Password<input name="password" type="password" autoComplete="current-password" required/></label><button type="submit">Sign in</button></form></details></section>;}
