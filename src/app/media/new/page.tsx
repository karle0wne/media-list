import Link from "next/link";
import { ProviderCover } from "@/components/provider-cover";import { requireSessionToken,requireUser } from "@/lib/auth";import { encodeCandidateToken } from "@/lib/candidate-token";import { scheduleMediaEnrichment } from "@/lib/enrichment-runtime";import { getSeriesSeasons,rawgConfigured,searchMediaByType,searchSeriesShows } from "@/lib/providers";import type { MediaCandidate,MediaType } from "@/lib/types";import { isMediaType } from "@/lib/user-media";import type { TmdbShowCandidate } from "@/lib/providers/tmdb";import { addCandidateAction } from "../../actions";
const baseCategories:Array<[MediaType,string]>=[["ANIME","Anime / donghua"],["MOVIE","Movies"],["SERIES","TV series"],["BOOK","Books"]];
export default async function AddPage({searchParams}:{searchParams:Promise<{type?:string;q?:string;show?:string;error?:string}>}){await requireUser();const sessionToken=await requireSessionToken();scheduleMediaEnrichment();const params=await searchParams;const gamesEnabled=rawgConfigured();const categories:Array<[MediaType,string]>=gamesEnabled?[...baseCategories,["GAME","Games"]]:baseCategories;const requestedType=isMediaType(params.type)?params.type:"ANIME";const type:MediaType=requestedType==="GAME"&&!gamesEnabled?"ANIME":requestedType;const q=params.q?.trim()??"";const showId=type==="SERIES"?params.show?.trim()??"":"";let results:MediaCandidate[]=[];let shows:TmdbShowCandidate[]=[];let searchError=params.error??"";try{if(q&&type!=="SERIES")results=await searchMediaByType(type,q);else if(showId)results=await getSeriesSeasons(showId);else if(q&&type==="SERIES")shows=await searchSeriesShows(q);}catch(error){searchError=error instanceof Error?error.message:"Search provider is temporarily unavailable";}
return <section><h1>Add media</h1><p className="muted">Choose a category, find the exact work or season, then add it to your list.</p>{!gamesEnabled&&<p className="muted">Game search becomes available when this installation configures a RAWG API key.</p>}{searchError&&<p className="error">{searchError}</p>}<form method="get" className="searchbar"><label className="srOnly" htmlFor="media-search-type">Media category</label><select id="media-search-type" name="type" defaultValue={type}>{categories.map(([value,label])=><option value={value} key={value}>{label}</option>)}</select><label className="srOnly" htmlFor="media-search-query">Search title</label><input id="media-search-query" name="q" defaultValue={q} placeholder={placeholder(type)} autoFocus={!q}/><button type="submit">Search</button></form>{showId&&<p><Link href={`/media/new?type=SERIES&q=${encodeURIComponent(q)}`}>← Back to series results</Link></p>}<div className="results">{shows.map(show=><article className="card result" key={show.externalId}><CoverSlot coverUrl={show.coverUrl} label="SERIES"/><div className="grow"><h2>{show.title}</h2><p className="muted">TV series · {show.year??"year ?"} · TMDB</p>{show.originalTitle&&show.originalTitle!==show.title&&<p className="resultAltTitle">{show.originalTitle}</p>}</div><Link className="button" href={`/media/new?type=SERIES&q=${encodeURIComponent(q)}&show=${encodeURIComponent(show.externalId)}`}>Choose season</Link></article>)}{results.map(item=><article className="card result" key={item.key}><CoverSlot coverUrl={item.coverUrl??null} label={item.type}/><div className="grow"><h2>{item.title}</h2><p className="muted">{item.type} · {item.year??"year ?"} · {item.source}</p>{item.credit&&<p className="resultCredit">{item.credit}</p>}{item.originalTitle&&item.originalTitle!==item.title&&<p className="resultAltTitle">{item.originalTitle}</p>}{item.romanizedTitle&&item.romanizedTitle!==item.title&&item.romanizedTitle!==item.originalTitle&&<p className="resultAltTitle">{item.romanizedTitle}</p>}</div><form action={addCandidateAction}><input type="hidden" name="candidateToken" value={encodeCandidateToken(item,sessionToken)}/><button type="submit" aria-label={`Add ${item.title}`}>Add</button></form></article>)}{!searchError&&q&&!showId&&!results.length&&!shows.length&&<p>No candidates found in {label(type)}.</p>}{!searchError&&showId&&!results.length&&<p>No seasons found for this series.</p>}</div></section>;}
function CoverSlot({coverUrl,label}:{coverUrl:string|null;label:string}){return <div className="resultCoverSlot">{coverUrl?<ProviderCover src={coverUrl} width={84} height={118}/>:<span>{label}</span>}</div>;}function placeholder(type:MediaType){return type==="BOOK"?"Book title":type==="MOVIE"?"Movie title":type==="SERIES"?"Series title":type==="GAME"?"Game title":"Anime title in any language";}function label(type:MediaType){return([...baseCategories,["GAME","Games"]]as Array<[MediaType,string]>).find(([value])=>value===type)?.[1]??type;}
