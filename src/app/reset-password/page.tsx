import { resetPasswordAction } from "../actions";
export default async function ResetPasswordPage({searchParams}:{searchParams:Promise<{token?:string;error?:string}>}){const{token="",error}=await searchParams;return <section className="card narrow"><h1>Reset password</h1><p className="muted">Set a new password for the account bound to this one-time link.</p>{error&&<p className="error">{error}</p>}{token?<form action={resetPasswordAction} className="stack"><input type="hidden" name="token" value={token}/><label>New password<input name="password" type="password" minLength={10} autoComplete="new-password" required autoFocus/></label><label>Confirm password<input name="confirmPassword" type="password" minLength={10} autoComplete="new-password" required/></label><button type="submit">Set new password</button></form>:<p className="error">Reset token is missing.</p>}</section>;}
