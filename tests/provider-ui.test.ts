import test from "node:test";import assert from "node:assert/strict";import { normalizeProviderCoverUrl } from "../src/components/provider-cover";import { externalMediaUrl } from "../src/lib/providers/urls";
test("RAWG library links target the concrete game",()=>{assert.equal(externalMediaUrl("RAWG","12345",""),"https://rawg.io/games/12345");});test("RAWG covers are resized before reaching the browser",()=>{assert.equal(normalizeProviderCoverUrl("https://media.rawg.io/media/games/abc/cover.jpg"),"https://media.rawg.io/media/resize/200/-/games/abc/cover.jpg");});test("TMDB covers stay on the bounded w185 size",()=>{assert.equal(normalizeProviderCoverUrl("https://image.tmdb.org/t/p/original/foo.jpg"),"https://image.tmdb.org/t/p/w185/foo.jpg");});
